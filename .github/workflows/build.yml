name: Build Flutter APK

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'

      - name: Setup Android Project & Permissions
        run: |
          rm -rf android app build .dart_tool build.gradle.kts settings.gradle.kts gradle.properties
          mkdir -p assets/images assets/jewellery
          flutter create --platforms=android --org com.roldygoldy --project-name roldygoldy_ui .
          chmod +x android/gradlew
          chmod -R 777 android
          flutter pub get

      - name: Build Debug APK
        run: flutter build apk --debug

      - name: Upload APK
        uses: actions/upload-artifact@v4
        with:
          name: RoldyGoldy-Flutter-APK
          path: build/app/outputs/flutter-apk/app-debug.apk
